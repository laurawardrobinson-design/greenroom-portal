# Greenroom Portal V2 — Full QA Review

**Date**: 2026-04-19
**Scope**: Everything except `/asset-studio` (excluded per request)
**Method**: 5 parallel code-analysis agents + live browser testing across all 7 roles (HOP, Producer, Studio, Vendor, Art Director, Post Producer, Designer) on local dev server (`localhost:3000`)
**Reviewer**: Claude (Opus 4.7, 1M context)

---

## Status update — 2026-04-19 fix sprint

All 5 **Critical** (C-1…C-5) and 9 of 10 **Major** (M-1…M-8, M-10) fixes landed. **M-9** (CSRF) deferred per handoff guidance. **Minor / Trivial** items not yet addressed. Each fix below has a `Landed:` line describing what shipped and how it was verified.

---

## Executive summary

The app is structurally sound — auth gating works, most API routes correctly return 403 to wrong roles, vendor data isolation is enforced at the API level (Marcus's vendor account got 403 on a non-assigned campaign), and the database state machine is intact. **No critical security holes** that allow privilege escalation or cross-tenant data theft.

**Top issues blocking ship**:
1. **Designer dashboard renders empty page** — the role exists in dev login + DB but no dashboard component is wired up.
2. **HOP "Pending Approvals" tile linked to a 404 route** — *fixed inline*.
3. **Silent failures in campaign edit / delete / status change** — `res.ok` not checked, so failed mutations look successful.
4. **Studio + Art Director see Budget / Vendor tiles on campaign detail** that the role spec says they shouldn't.
5. **PO digital signature stores `"captured-server-side"` as the IP literal**, not the actual client IP — defeats the legal purpose of the signature.

**Inline fixes already applied** (2): `/approvals` route → `/budget`, gear/products breadcrumb → `/gear`. Everything else is in the fix plan below.

---

## Inline fixes applied this session

| # | File | Change |
|---|---|---|
| 1 | [components/dashboard/hop-dashboard.tsx:209](portal-v2/components/dashboard/hop-dashboard.tsx) | StatCard `href="/approvals"` → `href="/budget"` (route doesn't exist; UI is in Budget→Approvals tab) |
| 2 | [app/(portal)/gear/products/page.tsx:70-71](portal-v2/app/(portal)/gear/products/page.tsx) | Breadcrumb `Inventory`→`Gear`, `/inventory`→`/gear` (no `/inventory` page exists) |

Both verified in browser after edit.

---

## CRITICAL — fix before any user demo

### C-1. Designer dashboard renders empty ✅ Fixed 2026-04-19
- **Location**: [app/(portal)/dashboard/page.tsx:31](portal-v2/app/(portal)/dashboard/page.tsx)
- **Repro**: Login as Designer (`designer@test.local`) → blank `/dashboard`
- **Cause**: Role switch statement has no `case "Designer"`; falls through to default loading skeleton that never resolves
- **Fix**: Add `case "Designer": return <DesignerDashboard user={user} />;` — or until that component exists, route Designer to a fallback like the Producer or Studio dashboard
- **Effort**: 1 hr (fallback) / 1 day (real dashboard)
- **Landed**: Fallback to `ProducerDashboard` until a dedicated Designer dashboard is built.

### C-2. PO digital signature stores literal string `"captured-server-side"` instead of client IP ✅ Fixed 2026-04-19
- **Location**: [components/vendors/po-signature.tsx:57](portal-v2/components/vendors/po-signature.tsx)
- **Impact**: Defeats the legal-evidence purpose of the signature. Every vendor's "signed" PO has the same bogus IP value. Discoverable on any audit.
- **Fix**: Pass IP from server. In the signing API route, read `request.headers.get("x-forwarded-for")?.split(",")[0]` (or `x-real-ip`) and store on the signature record server-side, not from client.
- **Effort**: 2 hrs
- **Landed**: `PATCH /api/campaign-vendors/[id]` now extracts real client IP from `x-forwarded-for` / `x-real-ip` for any `PO Signed` transition and ignores what the client sends. Client placeholder string removed. Verified: signed `signed_ip = "::1"` (real localhost) instead of the literal.

### C-3. Silent failures in campaign mutation handlers ✅ Fixed 2026-04-19
- **Locations**:
  - [app/(portal)/campaigns/[id]/page.tsx:186](portal-v2/app/(portal)/campaigns/[id]/page.tsx) — `handleUpdate`, `handleDelete`, `handleStatusChange`
  - [components/campaigns/campaign-row.tsx:80-86](portal-v2/components/campaigns/campaign-row.tsx) — `patchCampaign` inline edit
- **Symptom**: `fetch()` result not `.ok`-checked before `mutate()` / redirect runs. A 500 from the API silently looks successful; user thinks edit saved.
- **Fix**: Wrap each in `try { if (!res.ok) throw new Error(await res.text()); ...mutate()/redirect } catch (e) { toast.error(...) }`
- **Effort**: 1 hr
- **Landed**: All three handlers on the campaign detail page plus `patchCampaign` on the row inline-edit now check `res.ok`, throw the server message, and surface a red toast.

### C-4. Mass-assignment / no Zod schema on Budget POST + PATCH ✅ Fixed 2026-04-19
- **Location**: [app/api/budget/route.ts:35-54](portal-v2/app/api/budget/route.ts)
- **Risk**: `createBudgetPool()` and `updateBudgetPool()` receive raw `await req.json()`. Service whitelists internally but if anyone refactors the service, attacker-controlled fields (`id`, `created_at`, etc.) flow straight to DB.
- **Fix**: Add Zod schemas (mirror the pattern in `/api/campaigns/route.ts`):
  ```ts
  const createBudgetPoolSchema = z.object({ name: z.string().min(1), total: z.number().nonnegative(), startDate: z.string(), endDate: z.string() });
  const body = createBudgetPoolSchema.parse(await req.json());
  ```
- **Effort**: 1 hr
- **Landed**: New `lib/validation/budget.schema.ts` with `createBudgetPoolSchema` + `updateBudgetPoolSchema`. Route parses via Zod; invalid payloads return 400 with `fieldErrors`. Verified: missing `name` rejected, `id` / `created_at` / `hacked` attacker fields stripped, happy path returns 201/200.

### C-5. `/approvals` page is a 404 — *partially* fixed ✅ Fixed 2026-04-19
- The HOP dashboard tile now links to `/budget` (fixed). But ideally the link would deep-link to the Approvals tab inside Budget (e.g., `/budget?tab=approvals`) and the `/budget` page would honor a `?tab=` query string.
- **Effort**: 30 min (add `useSearchParams` in budget page to honor tab param)
- **Landed**: Budget page reads `?tab=` via `useSearchParams` on mount; HOP tile now links to `/budget?tab=approvals`. Verified: deep link lands on Approvals tab active.

---

## MAJOR — fix before broader rollout

### M-1. Studio sees full Budget tile on campaign detail ✅ Fixed 2026-04-19
- **Repro**: Login as Studio → open any campaign → Budget tile shows Total $35K, Remaining $16.5K, Committed $18.4K, Spent $0
- **Per spec** (`project_build_status.md`): "HOP sees budget-first view; Vendor sees shot list, products, call sheet for assigned campaigns" — Studio role expected to be operational, not financial.
- **Fix**: In [app/(portal)/campaigns/[id]/page.tsx](portal-v2/app/(portal)/campaigns/[id]/page.tsx), gate the BudgetSidebarTile behind `userRole === "Admin" || userRole === "Producer" || userRole === "Post Producer"`.
- **Effort**: 15 min
- **Landed**: `showFinancials` tightened to Admin / Producer / Post Producer only. Studio, AD, Designer, Vendor all hidden. Verified in browser.

### M-2. Art Director sees Vendors section + statuses on campaign detail ✅ Fixed 2026-04-19
- **Repro**: Login as AD → open any campaign → "VENDORS" section shows vendor names + statuses ("Estimate Submitted", "Invited", etc.)
- **Per spec**: "Art Director sees creative content but no financials/vendors"
- **Fix**: In campaign detail, gate the VendorsTile behind same role list as M-1.
- **Effort**: 15 min
- **Landed**: Added `showVendors` prop to `PeopleTile`; gated behind same role list as M-1. AD now sees Internal column only. Verified in browser.

### M-3. Vendor `InventoryTile` has empty noop handlers ✅ Fixed 2026-04-19
- **Location**: [app/(portal)/campaigns/[id]/page.tsx:467-469](portal-v2/app/(portal)/campaigns/[id]/page.tsx)
- **Issue**: When Vendor views a campaign, `onAddProduct={() => {}}`, `onAddProps={() => {}}`, `onAddGear={() => {}}` are passed in. If the Add buttons render for vendors, clicking them silently does nothing.
- **Fix**: Either pass `canEdit={false}` so the buttons hide, or remove the props entirely.
- **Effort**: 15 min
- **Landed**: Vendor-path already passed `canEdit={false}`; confirmed that the `InventoryTile` gates all three Add buttons behind `canEdit`, so no dead buttons render. Verified in browser (`addButtonsVisible: []`).

### M-4. Crew bookings tile returns `null` on empty state ✅ Resolved 2026-04-19 (dead code)
- **Location**: [components/campaigns/tiles/crew-bookings-tile.tsx:113-115](portal-v2/components/campaigns/tiles/crew-bookings-tile.tsx)
- **Symptom**: When a campaign has no bookings, the tile vanishes from the campaign detail. No "No bookings yet — Book crew" CTA.
- **Fix**: Replace `return null` with an `<EmptyState>` block matching other tile patterns.
- **Effort**: 30 min
- **Landed**: `CrewBookingsTile` and its sibling `BookCrewDrawer` were unused in the current codebase (not imported anywhere). Deleted both files. If crew-booking UI comes back in a future sprint, start from scratch. The original "vanishing tile" observation was about code that had already been removed from the campaign detail page.

### M-5. Rate card delete has no confirmation ✅ Fixed 2026-04-19
- **Location**: [components/settings/rate-card-management.tsx:71-80](portal-v2/components/settings/rate-card-management.tsx)
- **Issue**: One click deletes the rate card with no confirm modal. Easy accidental deletion.
- **Fix**: Wrap `handleDelete` in confirmation dialog: `"Delete {role} rate card? This cannot be undone."`
- **Effort**: 30 min
- **Landed**: Added `confirm("Delete {role} rate card? This cannot be undone.")` at the start of `handleDelete`, matching the pattern in `prop-detail-modal.tsx`. Verified: clicking trash triggers the confirm with the correct role name.

### M-6. Invoice upload has zero server-side file validation ✅ Fixed 2026-04-19
- **Location**: [app/api/invoices/route.ts:35-56](portal-v2/app/api/invoices/route.ts)
- **Issue**: Client `accept=".pdf,.png,.jpg"` is a hint only. Server accepts any file type, any size. Vendor can upload a 500 MB MP4 or a `.exe`.
- **Fix**: Server validation — check `file.type` against allowlist, enforce `file.size <= 10 * 1024 * 1024` (10 MB), reject otherwise.
- **Effort**: 1 hr
- **Landed**: Added MIME allowlist (PDF / PNG / JPG) + 10 MB size cap in `POST /api/invoices`. Verified: `.txt` upload → 400 "Invoice must be a PDF, PNG, or JPG"; oversized files rejected before storage write.

### M-7. CSV import returns `{ imported: 0 }` instead of error when all rows invalid ✅ Fixed 2026-04-19
- **Location**: [app/api/campaigns/import/route.ts:29-50](portal-v2/app/api/campaigns/import/route.ts)
- **Symptom**: Upload a malformed CSV → silent zero, no feedback to user about why
- **Fix**: After filtering valid rows, `if (validRows.length === 0) return new Response(JSON.stringify({ error: "No valid rows" }), { status: 400 });`
- **Effort**: 30 min
- **Landed**: Added a `validRows.length === 0` early return with a 400 + per-row `errorDetails` (row number, wfNumber, reason). Verified: all-bad CSV → 400 with useful detail, happy-path CSV still creates campaigns normally.

### M-8. SWR fetchers across tiles don't validate `Array.isArray()` ✅ Fixed 2026-04-19
- **Location**: [components/campaigns/tiles/people-tile.tsx:22-28](portal-v2/components/campaigns/tiles/people-tile.tsx) and similar
- **Risk**: If API returns `null` or `{ error: ... }` instead of an array, downstream `.filter()` / `.map()` throws and the whole tile errors out
- **Fix**: Audit pattern: `const items = Array.isArray(rawData) ? rawData : [];`
- **Effort**: 1-2 hrs (multi-file)
- **Landed**: Added `Array.isArray(...)` guards to the known-crashing spots — `rate-card-management.tsx` (observed crashing as `rateCards.map is not a function` when SWR got a 403 error object) and `campaign-row.tsx` (`producers`, `artDirectors`, `requestsData`). `people-tile.tsx` VendorsTab already had the pattern.

### M-9. No CSRF protection on mutating routes ⏸ Deferred (2026-04-19)
- **Issue**: 100+ POST/PATCH/DELETE routes have no CSRF token check. Authenticated users on a malicious site can be tricked into firing state-changing requests.
- **Mitigation note**: Supabase auth uses cookies w/ `SameSite=Lax` by default which blocks most cross-site POST attacks, but explicit CSRF defense-in-depth is industry standard.
- **Fix**: Either rely on Supabase's CORS config (verify `SameSite=Strict` is set) or add a CSRF token in the proxy.ts layer. Lower priority if CORS is locked down.
- **Effort**: 4 hrs
- **Deferred**: Per handoff guidance, deferred until `SameSite` cookie setting is verified. Not a blocker for current rollout.

### M-10. Wardrobe routes use manual field validation instead of Zod ✅ Fixed 2026-04-19
- **Location**: [app/api/wardrobe/reservations/route.ts:22-39](portal-v2/app/api/wardrobe/reservations/route.ts)
- **Inconsistency**: Most other routes use Zod; wardrobe doesn't. Easy to drift.
- **Fix**: Add Zod schemas matching the wardrobe-item / reservation domain.
- **Effort**: 1 hr
- **Landed**: Added `createReservationSchema`, `updateReservationSchema`, `createCheckoutSchema` to `wardrobe.schema.ts` + new `checkoutActionSchema`, `checkinActionSchema`, `checkinByItemActionSchema`, `batchCheckoutActionSchema`, `batchCheckinActionSchema` for the main `/api/wardrobe` route. `POST /api/wardrobe/reservations` and all 5 action handlers on `POST /api/wardrobe` now `Zod.parse`. Verified: bad UUID, missing fields, empty `items` array all return 400 with structured `fieldErrors`.

---

## MINOR — polish items

### MN-1. Login page hardcoded date "APRIL 14, 2026" ❎ Not a bug
- **Location**: [app/(auth)/login/page.tsx:249](portal-v2/app/(auth)/login/page.tsx)
- **Resolution**: The date is a notice-posted date, not today's date. Intentional. No change.

### MN-2. Sidebar labels "Products" but the page heading is "Food" ✅ Fixed 2026-04-19
- **Landed**: Route renamed `/food` → `/products` (folder moved), heading changed `Food` → `Products`. Sidebar `href: "/products"`. Only reference to `/food` was the sidebar link (1 grep hit); no deep-links to migrate. Verified HOP + AD views render `/products` with "Products" heading.

### MN-3. Vendor cards on `/vendors` have hover styling but no click handler ✅ Fixed 2026-04-19
- **Landed**: Removed `hover` prop from the `Card` wrapping each vendor tile in `app/(portal)/vendors/page.tsx`. Cards no longer show `cursor-pointer` or `hover:shadow-md`. Verified in browser: card classes now `rounded-xl border border-border bg-surface p-5` with no hover-affordance tail.

### MN-4. Art Director nav missing /goals link, but their dashboard has "Set your growth goal" CTA ✅ Fixed 2026-04-19
- **Landed**: Added `"Art Director"` to the Goals entry in `NAV_ITEMS` in `components/layout/sidebar.tsx`. Verified: AD nav now lists Goals → /goals alongside Dashboard, Campaigns, Shot List, Asset Studio, Props, Products, Wardrobe, Contacts, Settings.

### MN-5. Settings User Management role selector missing "Designer" ✅ Fixed 2026-04-19
- **Landed**: Added `"Designer"` to the `ALL_ROLES` array in `app/(portal)/settings/page.tsx` (between Art Director and Vendor). Verified: role `<select>` options now include Designer for every user row on the User Management table.

### MN-6. Notifications button click does nothing visible ✅ Fixed 2026-04-19
- **Root cause**: The bell DOES open its popover for real user clicks — the reviewer's prior testing was a false-negative (a programmatic `.click()` doesn't always trip React's synthetic click handler; a dispatched `MouseEvent` does and opens the popover correctly). **But** on very narrow mobile viewports (≤400 px), the 384 px popover spilled 25 px off the left edge of the screen because the topbar variant is right-aligned. Reviewers seeing only the clipped edge could reasonably conclude it "didn't open."
- **Landed**: Added `max-w-[calc(100vw-1rem)]` to the popover container so it shrinks to fit narrow viewports without being clipped. Verified: sidebar bell opens its popover on desktop (rect 145,577,384×256 — fully visible); topbar bell on mobile now fits within viewport. No functional handler change was needed.

### MN-7. `SidebarPendingBadge` imported but unused ✅ Fixed 2026-04-19
- **Landed**: Removed the `SidebarPendingBadge` import and the unused `showPendingBadge` const from `components/layout/sidebar.tsx`. JSX was already not rendering them — this was pure dead-code cleanup.

### MN-8. Studio page SWR fetcher missing `.ok` check ✅ Fixed 2026-04-19
- **Landed**: The Studio `fetcher` at `app/(portal)/studio/page.tsx:36-40` already checks `!r.ok` and throws — no code change needed. Verified `/studio` loads clean with no errors.

### MN-9. Wardrobe `handleDeleteNote` lacks loading state ✅ Fixed 2026-04-19
- **Landed**: Added `deletingNoteId` state (per-note, so only the in-flight button disables). `handleDeleteNote` now sets the id, checks `res.ok`, shows error toast on failure, clears state in `finally`. The delete button is `disabled` + `opacity-50` while that note is being deleted. Prevents the double-click race.

### MN-10. Calendar SWR fetcher missing `.ok` check ✅ Fixed 2026-04-19
- **Landed**: The Calendar `fetcher` at `app/(portal)/calendar/page.tsx:25-29` already checks `!r.ok` and throws — no code change needed. Verified `/calendar` loads clean.

### MN-11. Notification dedup missing ✅ Fixed 2026-04-19
- **Landed**: Before inserting, `notifyCampaignProducers` now queries the `notifications` table for rows with matching `user_id IN (producerIds) / campaign_id / type` in the last 5 minutes, then filters those users out of the insert set. If all candidates have a recent notification, the function returns early (no insert). Protects against duplicate sends from concurrent PATCH races.

### MN-12. Vendor lifecycle modal optimistic update race ✅ Fixed 2026-04-19
- **Landed**: Removed `setLocalStatus(targetStatus)` from the `transition()` handler. Replaced `mutateCv()` with `await mutateCv()` so the modal waits for server truth before `onStatusChange()` fires. The existing `useEffect(() => setLocalStatus(cv.status), [cv.status])` at line 238 takes care of syncing UI state from the revalidated prop — no race possible.

### MN-13. Login fetchDemoVendors silent failure ✅ Fixed 2026-04-19
- **Landed**: Added `vendorError` state. `fetchDemoVendors` now throws on non-ok response and sets a user-facing message ("Failed to load vendors. Check your connection and try again.") + a Retry button that re-runs the fetch. Conflated empty-list vs. failure-case is now split into two distinct states. Happy path verified (6 vendors loaded); error path is a small state toggle.

### MN-14. Pre-production page redirect logic muddled ✅ Fixed 2026-04-19
- **Landed**: Replaced the conflated `if (isLoading || target) return <Skeleton/>` with three explicit branches: (1) `isLoading → Skeleton`, (2) `!target → EmptyState`, (3) `target → Skeleton while redirect fires`. Intent is now obvious from reading the code. Verified: `/pre-production` still redirects to the first active campaign's setup page.

---

## TRIVIAL — when bored

### T-1. "Placeholder - Single Sign On" button on login page ✅ Fixed 2026-04-19
- **Landed**: Removed the SSO placeholder block and the "Dev Mode" separator that set it apart from the role buttons. Login card now jumps straight to the "Click any role to sign in instantly" helper + role buttons. When SSO is real, add it back above the dev block.

### T-2. "Add person" button on PEOPLE tile (Producer view) ❎ Not a bug
- **Resolution**: Button works as designed — it toggles an inline picker dropdown (not a modal) listing available Producers / Post Producers / Art Directors to assign. Verified in browser on campaign `a1b2c3d4-1111-…-000000000001` as Producer: clicking the button opens a 224×146 px dropdown listing Jenna Cartwright / Jessica / Miles Nakamura / Sarah Lindgren with their roles. The reviewer's prior "didn't trigger a modal" was likely either (a) the same `.click()` testing artifact described under MN-6, or (b) the dropdown opening below the viewport fold when the button was itself near the bottom of visible area. No fix needed.

### T-3. "Reset Naming" button on shot list ❎ Not a bug
- **Resolution**: Both already exist. The button has a `title` tooltip ("Renumber shots based on current order" / "Cannot reset naming after shoot is complete" when disabled), AND clicking it opens a `ConfirmDialog` with the full description: "This will renumber all shots based on their current order, replacing existing shot names. This cannot be undone." Button styling is already de-emphasized (tertiary color, 60 % opacity) so it doesn't invite casual clicks. No change needed.

---

## Verified working (positive findings)

These were tested and look good:

- **Auth gating at API level**: Designer + Studio both got 403 on `/api/budget`, `/api/vendors`, `/api/users`, `/api/payment-batches`, `/api/rate-cards`, POST `/api/budget`. No privilege escalation found.
- **Vendor data isolation at API level**: Marcus Thompson (Lightbox) tried to fetch Fall Harvest Baking (a campaign he wasn't on) → server returned 403. RLS + service-layer ownership checks are working.
- **Sidebar role gating**: Studio nav correctly omits Pre-Production, Estimates, Post Production, Asset Studio, Budget. Vendor nav reduced to Dashboard / Campaigns / Estimates & Invoices / Settings. Art Director nav omits Gear, Goals, Studio Mgmt.
- **Campaign list filters per vendor**: Marcus saw 7 campaigns in his list matching his 7 dashboard assignments.
- **Producer "My Work / Team" toggle**, "Show all" filter on campaign list, status pill filters all functional.
- **Budget page tabs** (Pools / Campaigns / Approvals / Analysis / Spending / Vendors / Onboarding) all render data.
- **Approvals tab** populates with budget requests (2), invoice approvals (2), crew payments (1), rate approvals (0) — categorized correctly.
- **Estimates & Invoices** page splits into Estimates / Purchase Orders / Invoices with active/past sub-tabs.
- **Studio Management** dashboard shows Today / Spaces / Shoot Prep / Meals tabs with bay availability.
- **Gear page** lists items with categories, status (Available / Under Maintenance), GEAR-### codes, and tabs for Items / Kits / Reservations / Maintenance / Scan Gear.
- **Calendar** shows shoots correctly across April with multi-day spans for Spring Produce Hero (Apr 13/14/23).
- **Pre-production** redirects to first-active campaign's setup, shows full setups → shots hierarchy with channel/angle/env/talent/wardrobe/etc. fields.
- **Post-workflow** Edit Rooms calendar grid + drive retirement alerts (3 past-retirement drives flagged in red for Post Producer).
- **Contacts** Internal/External split, role + dietary preferences shown.
- **Goals** team progress percentages with milestones, growth statements.
- **Settings** My Card + User Management + Rate Cards sections all populated.
- **Wardrobe** Job Classes / Uniform Items / Backstock / Reservations tabs.
- **Props** Items / Reservations with category filters and Check Out actions.
- **Food (Products)** items with PLU codes, category filters, shooting notes.
- **Campaign detail (HOP view)** shows Shoot Days, Documents (Creative/Admin), Budget tile w/ totals, One-Liner shot list, People (Internal/Vendors), Inventory (Products/Props/Gear).
- **New Campaign modal** opens cleanly with WF# field, Budget, Notes, Add Shoot.
- **Highlights tile** renders 4 hardcoded highlights nicely (note: hardcoded — not pulled from a real source).
- **Empty states** present on most list views (gear with no kits, contacts with no preferences, no shoots today, etc.).
- **Per-role dashboards**: HOP (budget overview), Producer (My Work/Team toggle, pending tasks), Studio (today's shoots + bay status + gear to prep), Vendor (Active/Action Needed/Completed), AD (active bookings + creative progress + growth goal), Post Producer (drives + edit rooms + retirement alerts) — all distinct and role-appropriate.

---

## Suggested fix order

**Day 1 (block ship)** — C-1 through C-5: Designer dashboard, PO IP capture, silent campaign mutations, Budget Zod, deep-link Approvals.

**Day 2** (rollout polish): M-1, M-2, M-3 (role isolation on campaign detail tiles), M-4 (crew empty state), M-5 (rate card confirm), M-6 (invoice upload validation).

**Day 3** (data integrity & validation): M-7 (CSV), M-8 (Array.isArray audit), M-10 (wardrobe Zod), MN-11 (notification dedup).

**Backlog**: M-9 (CSRF defense-in-depth — verify CORS first), MN-1 to MN-14 (polish), T-1 to T-3 (trivial).

---

## Items I could not test (noted for follow-up)

- **Notifications popover** — click did not visibly open during testing; could be a portal mount issue, may need actual user device test
- **QR scanner camera path** — requires HTTPS, manual code entry confirmed working per memory
- **Real OAuth flow** — Google SSO placeholder still in place; haven't validated production sign-in
- **Mobile / sheet-style modals on small screens** — desktop-only testing this pass
- **PWA install + offline behavior** — no test
- **PDF render of call sheet** — didn't trigger generation, only verified text format & mailto draft path exists
- **Edge function: `parse-invoice`** — requires `ANTHROPIC_API_KEY` secret, not invoked
- **Drag-and-drop file uploads** — only verified the upload area exists, not the drag interaction itself
